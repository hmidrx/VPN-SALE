# Wallet And Ledger

Wallet balances are derived from immutable double-entry ledger entries. Corrections use compensating entries. Money uses integer minor units only. Database transactions, row locks, idempotency, reservations, and reconciliation prevent double spending and duplicate top-ups/refunds.
