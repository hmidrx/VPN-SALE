import{expect,test}from'@playwright/test';
test('manual card transfer is Toman-only and responsive',async({page})=>{await page.goto('/ui-preview/manual-topup');await expect(page.getByRole('heading',{name:'کارت‌به‌کارت',exact:true})).toBeVisible();await expect(page.getByText('حداقل ۱۰۰٬۰۰۰ تومان')).toBeVisible();await expect(page.getByText('پس از دریافت اطلاعات کارت از پشتیبانی، فیش واریز را ارسال کنید.').first()).toBeVisible();await expect(page.getByText(/ریال/)).toHaveCount(0);await expect(page.locator('body')).not.toContainText(/\b\d{16}\b/);const overflow=await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth);expect(overflow).toBe(false)});
