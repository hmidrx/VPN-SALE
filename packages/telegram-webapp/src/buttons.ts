import {getNative} from "./environment";import type {ButtonOptions,NativeButton} from "./types";
const registrations=new WeakMap<NativeButton,Set<()=>void>>();
export function registerButton(name:"BackButton"|"MainButton"|"SecondaryButton"|"SettingsButton",options:ButtonOptions):()=>void{const button=getNative()?.[name];if(!button)return()=>{};if(options.text!==undefined)button.setText?.(options.text);options.visible?button.show?.():button.hide?.();options.enabled===false?button.disable?.():button.enable?.();options.loading?button.showProgress?.():button.hideProgress?.();if(options.onClick){const owned=registrations.get(button)??new Set();if(!owned.has(options.onClick)){button.onClick?.(options.onClick);owned.add(options.onClick);registrations.set(button,owned)}}let active=true;return()=>{if(!active)return;active=false;if(options.onClick&&registrations.get(button)?.delete(options.onClick))button.offClick?.(options.onClick);button.hide?.();button.hideProgress?.()}}
