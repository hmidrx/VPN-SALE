import React from "react";import {cx} from "../internal/cx";
type Tone="neutral"|"primary"|"success"|"warning"|"danger"|"info";
export function Badge({tone="neutral",children}:{tone?:Tone;children:React.ReactNode}){return <span className={cx("ui-badge",`ui-${tone}`)}>{children}</span>} export const StatusBadge=Badge; export function RoleBadge({role}:{role:string}){return <Badge tone="info">{role}</Badge>}
export function Alert({title,tone="info",children}:{title:string;tone?:Tone;children?:React.ReactNode}){return <section className={cx("ui-alert",`ui-${tone}`)} role={tone==="danger"?"alert":"status"}><strong>{title}</strong>{children}</section>}
export function EmptyState({title,description,action}:{title:string;description?:string;action?:React.ReactNode}){return <section className="ui-empty"><h3>{title}</h3>{description&&<p>{description}</p>}{action}</section>}
export function Skeleton({label="Loading"}:{label?:string}){return <span className="ui-skeleton" role="status"><span className="ui-sr-only">{label}</span></span>} export function Spinner({label="Loading"}:{label?:string}){return <span className="ui-spinner" role="status" aria-label={label||undefined}/>} export function Progress({value,label}:{value:number;label:string}){return <label className="ui-field"><span>{label}</span><progress max={100} value={Math.max(0,Math.min(100,value))}/></label>}
export function Toast({children,tone="info",assertive=false}:{children:React.ReactNode;tone?:Tone;assertive?:boolean}){return <div className={cx("ui-toast",`ui-${tone}`)} role={assertive?"alert":"status"} aria-live={assertive?"assertive":"polite"} aria-atomic="true">{children}</div>}
