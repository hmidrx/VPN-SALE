import React from "react";import {cx} from "../internal/cx";import { DropdownMenu } from "../overlays";
export function Avatar({name,src}:{name:string;src?:string}){return src?<img className="ui-avatar" src={src} alt={name}/>:<span className="ui-avatar" aria-label={name}>{name.trim().slice(0,1)}</span>}
export function Card({interactive,className,...p}:React.HTMLAttributes<HTMLElement>&{interactive?:boolean}){return <article className={cx("ui-card",interactive&&"ui-card--interactive",className)} {...p}/>} export function InteractiveCard(p:React.AnchorHTMLAttributes<HTMLAnchorElement>){return <a {...p} className={cx("ui-card","ui-card--interactive",p.className)}/>}
export function StatCard({label,value,detail}:{label:React.ReactNode;value:React.ReactNode;detail?:React.ReactNode}){return <Card><span className="ui-muted">{label}</span><strong className="ui-stat">{value}</strong>{detail&&<small>{detail}</small>}</Card>}
export function PageHeader({eyebrow,title,actions}:{eyebrow?:string;title:string;actions?:React.ReactNode}){return <header className="ui-page-header"><div>{eyebrow&&<p className="ui-eyebrow">{eyebrow}</p>}<h1>{title}</h1></div>{actions}</header>} export function SectionHeader({title,action}:{title:string;action?:React.ReactNode}){return <header className="ui-section-header"><h2>{title}</h2>{action}</header>}
export function AppShell({sidebar,mobileNavigation,children,density="comfortable"}:{sidebar:React.ReactNode;mobileNavigation:React.ReactNode;children:React.ReactNode;density?:"comfortable"|"compact"}){return <div className={cx("ui-app-shell",`ui-app-shell--${density}`)}>{sidebar}<main id="main-content" className="ui-main">{children}</main>{mobileNavigation}</div>} export function UserMenu({name,role}:{name:string;role?:string}){return <DropdownMenu label={name}><span>{role}</span></DropdownMenu>}
