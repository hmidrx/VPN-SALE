import { ManagementShell, Tech } from "../../../../src/components/ManagementShell";
import { metadataEntries } from "../../../../src/management/format";
export default async function Page({ params }: { params: Promise<{ eventId: string }> }): Promise<React.ReactElement> { const { eventId } = await params; const meta=metadataEntries({ reason:"safe", token:"redacted" }); return <ManagementShell title="جزئیات رویداد حسابرسی" required="audit.read"><section className="panel stack"><p>ID: <Tech>{eventId}</Tech></p><p>کد: <Tech>event.code</Tech></p><p>Correlation: <Tech>correlation-id</Tech></p><h2>metadata امن</h2>{meta.map(([k,v])=><p key={k}><Tech>{k}</Tech>: <Tech>{v}</Tech></p>)}<p className="notice">raw HTML و کلیدهای secret-like رندر نمی‌شوند.</p></section></ManagementShell>; }
