import { FinanceShell, Money } from '../../../../../../src/components/FinanceShell'; import { Tech } from '../../../../../../src/components/ManagementShell';
export default async function Page({ params }: { params: Promise<{ walletId: string }> }): Promise<React.ReactElement> { const resolved = await params; return <FinanceShell title="اعتبار دستی مدیریتی" required="wallets.adjust"><form className="panel form-grid"><label>مبلغ مثبت ریال<input inputMode="numeric" pattern="[0-9]+" required /></label><label>کد علت<input maxLength={40} required /></label><label>شرح پاک‌سازی‌شده<textarea maxLength={240} required /></label><label>مرجع داخلی امن<input maxLength={80}/></label><p>کیف‌پول: <Tech>{resolved.walletId}</Tech></p><p>پیش‌نمایش تومان فقط نمایشی است: <Money value={1000}/></p><button className="btn" type="button">بازکردن تأیید صریح</button></form><p className="notice">Idempotency در حافظه و پنهان از DOM/لاگ تولید می‌شود؛ موفقیت قبل از commit سرور نمایش داده نمی‌شود.</p></FinanceShell>; }
