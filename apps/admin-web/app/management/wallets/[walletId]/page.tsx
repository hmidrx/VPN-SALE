import { ActionLinks, BalanceCards, FinanceShell } from '../../../../src/components/FinanceShell';
import { Tech, StatusBadge } from '../../../../src/components/ManagementShell';
export default async function Page({ params }: { params: Promise<{ walletId: string }> }): Promise<React.ReactElement> { const resolved = await params; return <FinanceShell title="جزئیات کیف‌پول" required="wallets.read"><section className="panel"><h2><Tech>{resolved.walletId}</Tech></h2><p>وضعیت مشتری و وضعیت کیف‌پول جداگانه نمایش داده می‌شوند.</p><StatusBadge value="wallet status from server"/></section><BalanceCards balance={{posted_balance_rial:0,reserved_balance_rial:0,available_balance_rial:0}}/><ActionLinks walletId={resolved.walletId}/><section className="panel"><h2>باکت‌ها، اعتبارها، رزروها و حسابرسی</h2><p>فقط فیلدهای امن API رندر می‌شود؛ account code در نمای عادی پنهان است.</p></section></FinanceShell>; }
