import { ManagementShell, ConfirmBox, StatusBadge, Tech } from "../../../../src/components/ManagementShell";
export default async function Page({ params }: { params: Promise<{ userId: string }> }): Promise<React.ReactElement> { const { userId } = await params; return <ManagementShell title="جزئیات مشتری" required="users.read"><section className="grid"><article className="panel"><h2>پروفایل امن</h2><p>User ID: <Tech>{userId}</Tech></p><StatusBadge value="ACTIVE"/><p>هویت تلگرام، first/last seen، وضعیت bot_started و blocked_bot فقط از API.</p></article><article className="panel"><h2>نشست‌های مشتری</h2><a className="pill" href="/management/sessions/customers">مدیریت نشست مشتری</a></article><article className="panel"><h2>تغییر وضعیت</h2><p>تعلیق/بلاک/غیرفعال‌سازی پیامد لغو نشست دارد و پیش از تأیید سرور خوش‌بینانه نمایش داده نمی‌شود.</p><ConfirmBox action="ارسال transition قانونی" /></article></section></ManagementShell>; }
