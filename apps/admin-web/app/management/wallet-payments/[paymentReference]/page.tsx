import { CommerceShell, TechRef } from '../../../../src/components/CommerceShell';
import { money } from '../../../../src/commerce/format';
export default async function Page({ params }: { params: Promise<{ paymentReference: string }> }): Promise<React.ReactElement> { const resolved = await params; return <CommerceShell title="بازرسی wallet-payment" required="wallets.read"><section className="panel"><h2><TechRef>{resolved.paymentReference}</TechRef></h2>{['order reference','invoice reference','customer','payment method','status','reservation reference','capture transaction/journal reference when ledger.read permits','refund transaction/journal reference when permitted','created/completed/failed/refunded timestamps','safe failure code','idempotency outcome without raw values'].map(x=><p key={x}>{x}: API</p>)}<p>amount {money(100000)}</p><p className="notice">capture/refund arbitrary/mark-success/payment amount edit/external gateway fields وجود ندارد.</p></section></CommerceShell>; }
