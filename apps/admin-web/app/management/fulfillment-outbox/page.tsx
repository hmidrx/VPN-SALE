import { CommerceShell, TechRef } from '../../../src/components/CommerceShell';
export default function Page(): React.ReactElement { const cols=['opaque event reference','event type','event version','order reference','status','attempt count','available-at','claimed-at','processed-at','last failure category','created-at','correlation ID']; return <CommerceShell title="بازرسی fulfillment outbox"><div className="table-wrap"><table className="data-table"><thead><tr>{cols.map(c=><th key={c}>{c}</th>)}</tr></thead><tbody><tr>{cols.map((c,i)=><td data-label={c} key={c}>{i===0?<a href="/management/fulfillment-outbox/API"><TechRef>API</TechRef></a>:'API'}</td>)}</tr></tbody></table></div><p className="notice">provider/server/inbound/credential/subscription payload و mark-processed/retry/provisioning controls نمایش داده نمی‌شود.</p></CommerceShell>; }
