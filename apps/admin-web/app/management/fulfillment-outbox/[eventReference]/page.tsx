import { CommerceShell, TechRef } from '../../../../src/components/CommerceShell';
import { safeMetadata } from '../../../../src/commerce/validation';
export default async function Page({ params }: { params: Promise<{ eventReference: string }> }): Promise<React.ReactElement> { const resolved = await params; const fields=['event type and version','order reference','exact product-version reference','normalized plan-selection summary','fulfillment-requirement schema version','status','attempts','timestamps','correlation ID','safe failure category']; return <CommerceShell title="جزئیات رویداد اوت‌باکس"><section className="panel"><h2><TechRef>{resolved.eventReference}</TechRef></h2>{fields.map(f=><p key={f}>{f}: API</p>)}<pre>{JSON.stringify(safeMetadata({product_version_id:'API', panel_url:'hidden', server_ip:'hidden'}), null, 2)}</pre><p className="notice">Suppress only if backend safe endpoint exists; processed provisioning work suppress نمی‌شود.</p></section></CommerceShell>; }
