import { CommerceShell, TechRef } from '../../../../src/components/CommerceShell';
import { money } from '../../../../src/commerce/format';
export default async function Page({ params }: { params: Promise<{ reservationReference: string }> }): Promise<React.ReactElement> { const resolved = await params; return <CommerceShell title="بازرسی reservation کیف‌پول" required="wallets.read"><section className="panel"><h2><TechRef>{resolved.reservationReference}</TechRef></h2>{['order','checkout','wallet/customer summary','status','purpose code','created time','expiration time','released time','captured time','contribution to reserved balance'].map(x=><p key={x}>{x}: API</p>)}<p>amount {money(100000)}</p><p className="notice">reservation پرداخت تکمیل‌شده نیست؛ create/capture/edit/release arbitrary وجود ندارد و release از workflow لغو می‌آید.</p></section></CommerceShell>; }
