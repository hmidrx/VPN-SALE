import { ServiceConsole } from "../service-console";

export default function Page(): React.ReactElement {
  return <ServiceConsole title="صف تحقق سفارش" focus="درخواست‌های تحقق تولیدشده از سفارش‌های پرداخت‌شده و مصرف دقیقاً یک‌بار" />;
}
