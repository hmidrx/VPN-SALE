import { CommerceShell } from '../../../src/components/CommerceShell';
export default function Page(): React.ReactElement { const cards=['orders awaiting wallet confirmation','reserved-payment orders','paid orders','orders ready for fulfillment','cancellation-requested orders','cancelled orders','refund-pending orders','refunded orders','failed orders','expired checkout sessions','active wallet reservations related to checkout','orders with reconciliation mismatch','failed fulfillment-outbox events','dead-letter fulfillment events','recent administrator cancellations']; return <CommerceShell title="نمای عملیات تجارت"><section className="cards">{cards.map(c=><article className="panel metric" key={c}><span>{c}</span><strong>API</strong><small>فقط با endpoint تجمیعی واقعی؛ بدون revenue، profit، conversion یا capacity ساختگی.</small></article>)}</section><section className="panel"><h2>مسیرهای اداری</h2><p><a href="/management/orders">کشف سفارش</a> · <a href="/management/invoices">فاکتورهای immutable</a> · <a href="/management/fulfillment-outbox">اوت‌باکس تحقق</a></p></section></CommerceShell>; }
