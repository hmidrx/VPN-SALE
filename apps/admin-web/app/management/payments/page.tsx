import { PaymentOpsShell } from '../../../src/components/PaymentOpsShell';
import { paymentPermissions } from '../../../src/payments/policy';
export default function Page(): React.ReactElement { return <PaymentOpsShell title="نمای کلی عملیات پرداخت" required={paymentPermissions.paymentsRead}><section className="cards"><article className="panel metric"><span>روش‌های فعال/متوقف/maintenance</span><strong>API</strong><small>فقط aggregate واقعی؛ بدون fetch همه رکوردها.</small></article><article className="panel metric"><span>Intentها و verification-pending</span><strong>API</strong><small>ریال و تومان مشتق‌شده فقط با aggregate امن.</small></article><article className="panel metric"><span>وبهوک‌ها و dead-letter</span><strong>API</strong><small>invalid signature و retry-pending read-only.</small></article></section><section className="panel"><h2>لینک‌های audit و Security Center</h2><p>با مجوزهای audit.read و security_events.read به رخدادهای امن مرتبط لینک می‌شود و metadata ناامن تکرار نمی‌شود.</p></section></PaymentOpsShell>; }
