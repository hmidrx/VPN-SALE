import { CommerceShell, TechRef } from '../../../../src/components/CommerceShell';
import { money } from '../../../../src/commerce/format';
export default async function Page({ params }: { params: Promise<{ checkoutReference: string }> }): Promise<React.ReactElement> { const resolved = await params; return <CommerceShell title="بازرسی checkout session" required="checkout.read"><section className="panel"><h2><TechRef>{resolved.checkoutReference}</TechRef></h2>{['customer','quote','order','payment method','status','currency','reservation reference','created time','expiration time','completed time','cancelled time','version','safe failure code','idempotency outcome without raw key'].map(x=><p key={x}>{x}: API</p>)}<p>amount {money(100000)}</p><p className="notice">read-only: completion/reopen/payment-method edit/amount edit/customer change/direct capture وجود ندارد.</p></section></CommerceShell>; }
