import { ManagementShell, ConfirmBox, StatusBadge, Tech } from "../../../../src/components/ManagementShell";
export default async function Page({ params }: { params: Promise<{ adminId: string }> }): Promise<React.ReactElement> { const { adminId } = await params; return <ManagementShell title="جزئیات مدیر" required="admins.read"><section className="grid"><article className="panel"><h2>پروفایل و وضعیت</h2><p>شناسه: <Tech>{adminId}</Tech></p><p>ایمیل: <Tech>normalized@example.com</Tech></p><StatusBadge value="ACTIVE"/><p>آخرین ورود، شکست ورود، lock_until و شمارنده شکست فقط از API نمایش داده می‌شوند.</p></article><article className="panel"><h2>نقش‌ها</h2><p><Tech>super_admin</Tech></p><p>حذف آخرین Super Admin و عملیات روی حساب فعلی در UI توضیح داده و توسط backend رد می‌شود.</p></article><article className="panel"><h2>نشست‌ها</h2><a className="pill" href="/management/sessions/admins">مدیریت نشست‌های مدیران</a></article><article className="panel"><h2>اقدام حساس</h2><ConfirmBox action="قفل/غیرفعال‌سازی پس از تأیید backend" /></article></section></ManagementShell>; }
