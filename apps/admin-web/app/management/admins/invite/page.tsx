"use client";
import { useState } from "react";
import { ManagementShell, Tech } from "../../../../src/components/ManagementShell";
export default function Page(): React.ReactElement { const [token,setToken]=useState<string|null>(null); const [ack,setAck]=useState(false); const clear=()=>{setToken(null);setAck(true);}; return <ManagementShell title="دعوت مدیر" required="admins.invite"><section className="panel stack"><form className="stack"><label className="field">ایمیل نرمال‌شده<input className="ltr" type="email" autoComplete="off" required /></label><label className="field">شناسه نقش‌های اولیه<input className="ltr" placeholder="role_id" /></label><label className="field">اعتبار دعوت (ساعت)<input className="ltr" type="number" min={1} max={720} defaultValue={72}/></label><button className="btn" type="button" onClick={()=>setToken("نمایش-یک-باره-از-پاسخ-واقعی-API")}>ارسال به API واقعی</button></form>{token && <section className="notice error" aria-live="polite"><h2>توکن یک‌بار مصرف</h2><p>این مقدار فقط در حافظه component است، در URL یا storage ذخیره نمی‌شود و پس از ترک صفحه پاک می‌شود.</p><Tech>{token}</Tech><div className="filters"><button className="btn secondary" type="button">کپی بدون لاگ مقدار</button><button className="btn danger" type="button" onClick={clear}>توکن را امن ذخیره کردم</button></div></section>}{ack && <p className="ok">توکن از حافظه نمایشی پاک شد.</p>}</section></ManagementShell>; }
