"use client";
import { useState } from "react";
import { AuthShell } from "../../../src/components/AuthShell";
import { login } from "../../../src/auth/api-client";
export default function LoginPage(): React.ReactElement { const [show,setShow]=useState(false); const [error,setError]=useState(""); const [loading,setLoading]=useState(false); async function submit(formData: FormData): Promise<void>{ setLoading(true); setError(""); try{ const result=await login(String(formData.get("email")),String(formData.get("password"))); if(result.outcome==="MFA_REQUIRED") location.assign("/auth/mfa"); else location.assign("/security/profile"); }catch{ setError("اطلاعات ورود معتبر نیست یا سرویس موقتاً در دسترس نیست."); } finally{ setLoading(false);} } return <AuthShell eyebrow="ورود مدیر" title="دسترسی امن به پنل مدیریت"><form action={submit} className="stack"><label className="field">ایمیل<input name="email" className="ltr" inputMode="email" autoComplete="email" required /></label><label className="field">گذرواژه<input name="password" type={show?"text":"password"} className="ltr" autoComplete="current-password" required /></label><button type="button" className="btn secondary" onClick={()=>setShow(!show)}>{show?"پنهان کردن":"نمایش گذرواژه"}</button>{error && <p className="notice error" role="alert">{error}</p>}<button className="btn" disabled={loading}>{loading?"در حال بررسی…":"ورود"}</button><p className="notice">توکن دسترسی فقط در حافظه نگهداری می‌شود و refresh cookie از نوع HttpOnly است.</p></form></AuthShell>; }
