import { AuthShell, SecurityNav } from "../../../src/components/AuthShell";
export default function SessionsPage(): React.ReactElement { const sessions=["دستگاه فعلی","نشست دوم آزمایشی"]; return <AuthShell eyebrow="مدیریت نشست" title="دستگاه‌ها و نشست‌ها"><SecurityNav/><div className="stack">{sessions.map((s,i)=><article className="session" key={s}><div><b>{s}</b><p className="ltr">created_at · last_used_at · expires_at</p>{i===0 && <span className="pill">دستگاه فعلی</span>}</div><button className={i===0?"btn secondary":"btn danger"}>لغو نشست</button></article>)}<button className="btn danger">لغو همه نشست‌ها</button></div></AuthShell>; }
