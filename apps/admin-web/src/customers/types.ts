export type CustomerStatus = "PENDING" | "ACTIVE" | "SUSPENDED" | "BLOCKED" | "DEACTIVATED";
export type CustomerSummary = { customer_reference: string; display_name: string | null; account_status: CustomerStatus; created_at: string; active_session_count: number; telegram: { id: string | number | null; username: string | null; display_name: string | null }; wallet: { status: string; available_balance_rial: number; posted_balance_rial: number; reserved_balance_rial: number; buckets: Array<{ bucket_type: string; balance_rial: number }> } | null; tags: Array<{ code: string; color_token: string; name_i18n: Record<string,string> }>; risk_indicators: string[]; version: number };
export type CustomerPage = { items: CustomerSummary[]; next_cursor: string | null };
export type CustomerDetail = { overview: CustomerSummary; security: { sessions: Array<{ session_reference: string; device_label: string | null; revoked_at: string | null; suspicious: boolean }> }; wallet: CustomerSummary["wallet"]; transactions: Array<{ journal_entry_reference: string; operation_code: string; posted_at: string; ledger_link: string }>; reservations: Array<{ reservation_reference: string; amount_rial: number; status: string }>; commerce: { orders: unknown[]; invoices: unknown[]; payments: unknown[]; refunds: unknown[] }; notes: Array<{ note_reference: string; note_type: string; body: string; pinned: boolean; version: number }>; activity: unknown[] };
