const rows = [
  ['مصرف', 'ارائه‌شده توسط API تجمیع‌شده، نه شمارنده خام پنل'],
  ['باقی‌مانده', 'نامحدود، ناشناخته و صفر جدا نمایش داده می‌شوند'],
  ['تازگی', 'زمان آخرین همگام‌سازی به‌صورت LTR نمایش داده می‌شود'],
]

export default function ServiceUsagePage() {
  return <main dir="rtl" className="usage-page"><h1>مصرف سرویس</h1><p>این صفحه فقط خلاصه امن مصرف، انقضا و هشدارهای سرویس را نمایش می‌دهد و هیچ اعتبارنامه، توکن اشتراک یا شناسه زیرساختی در مرورگر ذخیره نمی‌کند.</p><dl>{rows.map(([name, value]) => <div key={name}><dt>{name}</dt><dd>{value}</dd></div>)}</dl></main>
}
