"use client";
import React from "react";
import { getService,listServices,type ServiceDetail,type ServiceSummary } from "../services";
import { operationLabels,serviceStatus } from "../service-status";
const faDate=(value:string|null)=>value?new Intl.DateTimeFormat("fa-IR",{dateStyle:"medium"}).format(new Date(value)):"ثبت نشده";
const shortRef=(value:string)=>value.length>12?`${value.slice(0,6)}…${value.slice(-4)}`:value;
function remaining(value:string|null){if(!value)return null;return Math.max(0,Math.ceil((new Date(value).getTime()-Date.now())/86400000));}
export function ServicesPage():React.ReactElement{const [items,setItems]=React.useState<ServiceSummary[]|null>(null);const [error,setError]=React.useState(false);const [filter,setFilter]=React.useState("all");const [query,setQuery]=React.useState("");const [sort,setSort]=React.useState("newest");const [refreshed,setRefreshed]=React.useState<Date|null>(null);const load=React.useCallback(()=>{const c=new AbortController();setError(false);void listServices(c.signal).then(x=>{setItems(x);setRefreshed(new Date())}).catch(e=>{if((e as Error).name!=="AbortError")setError(true)});return()=>c.abort()},[]);React.useEffect(load,[load]);let shown=[...(items??[])].filter(x=>{const s=serviceStatus(x.lifecycle);return(filter==="all"||s.group===filter)&&(x.display_name.includes(query)||shortRef(x.service_reference).toLowerCase().includes(query.toLowerCase()))});shown.sort((a,b)=>sort==="expiry"?(a.expires_at?Date.parse(a.expires_at):Infinity)-(b.expires_at?Date.parse(b.expires_at):Infinity):sort==="active"?Number(serviceStatus(b.lifecycle).group==="active")-Number(serviceStatus(a.lifecycle).group==="active"):Date.parse(b.created_at)-Date.parse(a.created_at));return <section className="services-page"><header className="services-header"><div><span>مرکز خدمات مشتریان</span><h1>سرویس‌های من</h1><p>وضعیت، اعتبار و دسترسی امن همه سرویس‌های خریداری‌شده را اینجا مدیریت کنید.</p></div><a className="ui-button" href="/catalog/products">خرید سرویس جدید</a></header><div className="services-meta"><strong>{items?.length??0} سرویس</strong><span>{refreshed?`به‌روزرسانی ${new Intl.DateTimeFormat("fa-IR",{timeStyle:"short"}).format(refreshed)}`:"در حال دریافت"}</span><button onClick={load}>تازه‌سازی</button></div><div className="services-toolbar"><label><span className="sr-only">جستجوی سرویس</span><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="جستجو با نام یا شناسه کوتاه" /></label><select aria-label="مرتب‌سازی" value={sort} onChange={e=>setSort(e.target.value)}><option value="newest">جدیدترین</option><option value="expiry">نزدیک‌ترین انقضا</option><option value="active">فعال‌ها ابتدا</option></select><div className="service-filters" aria-label="فیلتر وضعیت">{[["all","همه"],["active","فعال"],["provisioning","در حال آماده‌سازی"],["expired","منقضی"],["suspended","متوقف"]].map(([v,l])=><button key={v} aria-pressed={filter===v} onClick={()=>setFilter(v)}>{l}</button>)}</div></div>{items===null&&!error?<div className="service-grid" aria-label="در حال بارگذاری"><i/><i/><i/></div>:error?<div className="services-notice"><h2>دریافت سرویس‌ها ممکن نشد</h2><p>اطلاعات قبلی تغییر نکرده است. اتصال را بررسی و دوباره تلاش کنید.</p><button onClick={load}>تلاش دوباره</button></div>:items?.length===0?<Empty/>:shown.length===0?<div className="services-notice"><h2>سرویسی با این مشخصات پیدا نشد</h2><button onClick={()=>{setFilter("all");setQuery("")}}>پاک‌کردن فیلترها</button></div>:<div className="service-grid">{shown.map(x=><ServiceCard key={x.service_reference} service={x}/>)}</div>}</section>}
function Empty(){return <article className="services-empty"><svg aria-hidden="true" viewBox="0 0 120 100"><path d="M60 8 103 29v28c0 22-16 34-43 43C33 91 17 79 17 57V29L60 8Z"/><path d="m42 54 12 12 25-29"/></svg><h2>هنوز سرویسی ندارید</h2><p>سرویس‌هایی که خریداری می‌کنید پس از ثبت امن سفارش در این بخش نمایش داده می‌شوند.</p><div><a className="ui-button" href="/catalog/products">مشاهده پلن‌ها</a><a href="/education">راهنمای انتخاب سرویس</a></div></article>}
function ServiceCard({service:s}:{service:ServiceSummary}){const st=serviceStatus(s.lifecycle),days=remaining(s.expires_at);return <article className={`service-card tone-${st.tone}`}><header><div><span>{st.label}</span><h2>{s.display_name}</h2></div><code title="شناسه عمومی سرویس">{shortRef(s.service_reference)}</code></header><p>{st.description}</p><dl><div><dt>{s.activated_at?"فعال‌سازی":"ثبت سفارش"}</dt><dd>{faDate(s.activated_at??s.created_at)}</dd></div>{s.expires_at&&<div><dt>انقضا</dt><dd>{faDate(s.expires_at)}{days!==null&&` · ${days} روز`}</dd></div>}<div><dt>اطلاعات اتصال</dt><dd>{s.delivery_ready?"آماده":"هنوز آماده نیست"}</dd></div></dl>{st.group==="provisioning"&&<div className="service-progress"><div><span>پیشرفت آماده‌سازی</span><b>{s.provisioning_progress}٪</b></div><progress max="100" value={s.provisioning_progress}/></div>}<a className="service-manage" href={`/services/${encodeURIComponent(s.service_reference)}`}>مدیریت سرویس <span>←</span></a></article>}
const tabs=[["overview","نمای کلی"],["connection","اتصال"],["usage","مصرف"],["manage","مدیریت"],["activity","فعالیت‌ها"]] as const;
export function ServiceDetailPage({serviceReference}:{serviceReference:string}):React.ReactElement{const [data,setData]=React.useState<ServiceDetail|null>(null),[error,setError]=React.useState(false),[tab,setTab]=React.useState<(typeof tabs)[number][0]>("overview");const load=React.useCallback(()=>{const c=new AbortController();setError(false);void getService(serviceReference,c.signal).then(setData).catch(e=>{if((e as Error).name!=="AbortError")setError(true)});return()=>c.abort()},[serviceReference]);React.useEffect(load,[load]);if(error)return <section className="services-notice"><h1>دریافت سرویس ممکن نشد</h1><a href="/services">بازگشت به سرویس‌ها</a><button onClick={load}>تلاش دوباره</button></section>;if(!data)return <div className="service-detail-loading" aria-label="در حال بارگذاری"/>;const s=data.summary,st=serviceStatus(s.lifecycle),days=remaining(s.expires_at);return <section className="service-detail"><header className="detail-top"><a href="/services" aria-label="بازگشت به سرویس‌ها">→</a><div><small>مدیریت سرویس</small><h1>{s.display_name}</h1></div><button onClick={load}>تازه‌سازی</button></header><nav className="service-tabs" role="tablist" aria-label="بخش‌های سرویس">{tabs.map(([v,l])=><button role="tab" aria-selected={tab===v} onClick={()=>setTab(v)} key={v}>{l}</button>)}</nav>{tab==="overview"&&<div className="detail-grid"><article className="service-hero"><span className={`status tone-${st.tone}`}>{st.label}</span><h2>{s.display_name}</h2><p>{st.description}</p><dl><div><dt>شناسه عمومی</dt><dd><code>{shortRef(s.service_reference)}</code> <button onClick={()=>void navigator.clipboard.writeText(s.service_reference)}>کپی</button></dd></div><div><dt>سلامت سرویس</dt><dd>{data.service_health}</dd></div><div><dt>فعال‌سازی</dt><dd>{faDate(s.activated_at)}</dd></div><div><dt>انقضا</dt><dd>{faDate(s.expires_at)} {days!==null&&`(${days} روز)`}</dd></div>{data.entitlement.device_limit!==null&&<div><dt>تعداد دستگاه</dt><dd>{data.entitlement.device_limit}</dd></div>}</dl></article><Actions data={data}/></div>}{tab==="connection"&&<article className="detail-panel"><h2>مرکز اتصال</h2>{!data.delivery.ready?<><h3>اطلاعات اتصال هنوز آماده نشده است</h3><p>سرویس همچنان در حال آماده‌سازی است. تا دریافت تحویل معتبر، لینک اشتراک یا QR نمایش داده نمی‌شود.</p><div className="panel-actions"><button onClick={load}>بررسی دوباره</button><a href="/support">پشتیبانی</a></div></>:<p>فرمت‌های تحویل معتبر پس از دریافت امن از سرور اینجا نمایش داده می‌شوند.</p>}</article>}{tab==="usage"&&<article className="detail-panel"><h2>مصرف سرویس</h2>{data.usage===null?<p className="unavailable">اطلاعات مصرف هنوز از سرور دریافت نشده است.</p>:null}</article>}{tab==="manage"&&<Actions data={data} expanded/>}{tab==="activity"&&<article className="detail-panel"><h2>فعالیت‌ها</h2>{data.latest_activity.length?<ol className="timeline">{data.latest_activity.map((x,i)=><li key={`${x.event}-${i}`}><strong>{x.label}</strong><time>{faDate(x.occurred_at)}</time></li>)}</ol>:<p>فعالیت تازه‌ای برای نمایش وجود ندارد.</p>}</article>}</section>}
function Actions({data,expanded=false}:{data:ServiceDetail;expanded?:boolean}){return <article className="detail-panel"><h2>{expanded?"مدیریت سرویس":"اقدام‌های پیشنهادی"}</h2><div className="operation-grid">{data.eligible_operations.map(x=><button key={x.operation_type} disabled={!x.eligible||x.billable} title={x.billable?"قیمت‌گذاری این عملیات هنوز فعال نشده است":undefined}>{operationLabels[x.operation_type]??"در حال بررسی"}<small>{x.billable?"قیمت‌گذاری هنوز فعال نیست":x.eligible?"آماده انجام":"در دسترس نیست"}</small></button>)}<a href="/support">پشتیبانی<small>گفت‌وگو با تیم پشتیبانی</small></a></div></article>}
