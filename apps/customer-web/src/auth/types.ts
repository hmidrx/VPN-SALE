export type CustomerAuthState = "INITIALIZING" | "TELEGRAM_DETECTED" | "AUTHENTICATING" | "AUTHENTICATED" | "REFRESHING" | "UNAUTHORIZED" | "EXPIRED" | "BLOCKED" | "SUSPENDED" | "DEACTIVATED" | "RATE_LIMITED" | "TELEGRAM_UNAVAILABLE" | "UNSUPPORTED_CLIENT" | "NETWORK_ERROR" | "SERVICE_UNAVAILABLE";
export type CustomerProfile = { customer_id: string; account_status: string; telegram_user_id: number | null; username: string | null; account_username: string | null; first_name: string | null; last_name: string | null; language_code: string | null; created_at: string; last_seen_at: string | null; current_session_id: string };
export type CustomerSession = { session_id: string; current: boolean; device_label: string | null; created_at: string; last_used_at: string | null; idle_expires_at: string; absolute_expires_at: string; revoked: boolean };
export type AuthResponse = { access_token?: string | null; csrf_token?: string | null; session_id?: string | null };
export type AuthCapabilities = { password_login: boolean; public_registration: boolean; telegram_login: boolean; telegram_linking: boolean; web_credential_enrollment: boolean; email_recovery: boolean; telegram_recovery: boolean; recovery_codes: boolean; manual_card_topups: boolean };
export type TelegramLinkChallenge = { challenge: string; expires_at: string };
export type ApiErrorCode = "validation_failure" | "registration_conflict" | "feature_unavailable" | "invalid_init_data" | "expired_init_data" | "rate_limited" | "unauthorized" | "expired" | "blocked" | "suspended" | "deactivated" | "csrf_failed" | "network_error" | "service_unavailable" | "malformed_response";
